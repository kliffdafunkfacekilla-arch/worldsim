# Your Design Vault

This directory is intended for storing design documents, lore, and narrative content related to the game world.

*   **`Chronicles/`**: Contains saga markdown files (e.g., `Saga Hero_Chronicle.md`) which are parsed by the `lore_pipeline.py` and `saga_parser.py` scripts to integrate narrative elements into the simulation's `narrative_quest_engine.py`.
