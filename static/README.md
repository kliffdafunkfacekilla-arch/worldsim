# Static Directory

This directory contains static files served by the FastAPI backend (`main.py`).

*   **`dashboard.html`**: A web-based observation dashboard for monitoring the simulation state. Accessible via `/static/dashboard.html` when the server is running.
